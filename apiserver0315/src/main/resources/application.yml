server:
  port: 80

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/adam
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: wnddkd

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
      springframework:
        security: trace

com:
  kakao:
    apiserver:
      secret: adam1234567890
